# ============================================================
# Variables para ejecutar el sistema con Docker Compose.
# Copia este archivo a ".env"  ->  cp .env.example .env
# El archivo ".env" real NO se versiona (está en .gitignore).
# ============================================================

# --- PostgreSQL ---
POSTGRES_DB=tienda_barrio_db
POSTGRES_USER=tienda_user
POSTGRES_PASSWORD=TbPostgres_9vK42mQxR7pL2026

# --- Backend (conexión a la BD por el nombre del servicio Docker: "postgres") ---
SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/tienda_barrio_db
SPRING_DATASOURCE_USERNAME=tienda_user
SPRING_DATASOURCE_PASSWORD=TbPostgres_9vK42mQxR7pL2026

# --- Seguridad (JWT) ---
JWT_SECRET=TbJwtSecret_8fC4wZp91LmQx73RsTiendaBarrioHernandez2026
JWT_EXPIRATION_MINUTES=480

# --- Puertos publicados en el host ---
FRONTEND_PORT=80
BACKEND_PORT=8080
POSTGRES_PORT=5432
